---
import versionInfo from '@root/version.json';
import Breadcrumb from '../../components/Breadcrumb.astro';
import CopyButton from '../../components/CopyButton.astro';
import Base from '../../layouts/Base.astro';

// Build-time domain reference: versionInfo.domains.website
const siteUrl = versionInfo.domains.website;
---

<Base
	title="Install Auto-Coursera — Load Unpacked in Under Two Minutes"
	description="Install Auto-Coursera by loading unpacked into Chrome, Edge, or Brave — no terminal, no admin privileges. Alternative terminal scripts also available."
	canonicalPath="/install/"
>
	<!-- ================================================================
	     HERO
	     ================================================================ -->
	<section class="hero" data-bg="deep" aria-labelledby="install-heading">
		<div class="container-narrow">
			<p class="hero__product">Auto-Coursera — AI-powered Coursera quiz assistant</p>
			<Breadcrumb items={[{ label: 'Home', href: '/' }, { label: 'Install' }]} />
			<h1 id="install-heading">Install Auto-Coursera</h1>
			<div class="hero__meta">
				<span class="version-badge">v{versionInfo.version}</span>
				<span class="hero__separator" aria-hidden="true">&middot;</span>
				<span class="hero__note">Free &amp; open source</span>
			</div>
			<p class="hero__lead">
				Load the extension directly into your browser in under two minutes —
				no terminal, no admin privileges, no scripts.
			</p>
			<div class="hero__actions">
				<a
					href={`https://github.com/${versionInfo.githubRepo}/releases/latest/download/auto-coursera.zip`}
					class="btn btn--primary"
				>Download extension zip</a>
			</div>
		</div>
	</section>

	<!-- ================================================================
	     AI ACCURACY DISCLAIMER
	     ================================================================ -->
	<section class="section section--flush" data-bg="deep" aria-labelledby="ai-disclaimer">
		<div class="container-narrow">
			<aside class="callout callout--warning" aria-labelledby="ai-disclaimer">
				<p class="callout__title" id="ai-disclaimer">AI answers are not guaranteed</p>
				<p>
					Auto-Coursera uses AI models that can and do make mistakes.
					Always review the highlighted answers before submitting —
					especially on graded assessments.
				</p>
			</aside>
		</div>
	</section>

	<!-- ================================================================
	     LOAD UNPACKED STEPS
	     ================================================================ -->
	<section class="section" data-bg="void" aria-labelledby="load-unpacked-heading">
		<div class="container-narrow">
			<h2 id="load-unpacked-heading">Load Unpacked — step by step</h2>
			<img
				src="/images/install-flow.svg"
				alt=""
				aria-hidden="true"
				class="install-flow-img"
				loading="lazy"
				width="640"
				height="160"
			/>

			<ol class="steps">
				<li class="step">
					<h3 class="step__title">
						<span class="step__number">1</span>
						Download the extension
					</h3>
					<p>
						Download the latest extension zip from GitHub Releases.
					</p>
					<p>
						<a
							href={`https://github.com/${versionInfo.githubRepo}/releases/latest/download/auto-coursera.zip`}
							class="btn btn--primary"
						>Download auto-coursera.zip</a>
					</p>
				</li>

				<li class="step">
					<h3 class="step__title">
						<span class="step__number">2</span>
						Extract the zip
					</h3>
					<p>
						Extract the downloaded zip file to a permanent location on your computer.
					</p>
					<p style="margin-inline-start: calc(2rem + var(--space-3));"><strong>Keep this folder permanently</strong> — your browser references it directly. Deleting or moving it will break the extension.</p>
					<p class="code-note" style="margin-inline-start: calc(2rem + var(--space-3));">
						The zip contains a <code>dist</code> folder with the extension files.
					</p>
				</li>

				<li class="step">
					<h3 class="step__title">
						<span class="step__number">3</span>
						Open your browser's extensions page
					</h3>
					<div class="step__tabbed" style="margin-inline-start: calc(2rem + var(--space-3));">
						<div class="install-tabs" data-tab-group="browser" role="tablist" aria-label="Browser extension page URLs">
							<button id="tab-browser-chrome" class="platform-tabs__tab" role="tab" data-tab="chrome" aria-controls="browser-panel-chrome" aria-selected="true" tabindex="0">Chrome</button>
							<button id="tab-browser-edge" class="platform-tabs__tab" role="tab" data-tab="edge" aria-controls="browser-panel-edge" aria-selected="false" tabindex="-1">Edge</button>
							<button id="tab-browser-brave" class="platform-tabs__tab" role="tab" data-tab="brave" aria-controls="browser-panel-brave" aria-selected="false" tabindex="-1">Brave</button>
						</div>

						<div class="platform-panel" id="browser-panel-chrome" role="tabpanel" aria-labelledby="tab-browser-chrome" data-tab="chrome">
							<p>Navigate to:</p>
							<div class="code-block">
								<pre><code>chrome://extensions</code></pre>
								<CopyButton text="chrome://extensions" label="Copy Chrome extensions URL" />
							</div>
						</div>
						<div class="platform-panel" id="browser-panel-edge" role="tabpanel" aria-labelledby="tab-browser-edge" data-tab="edge" hidden>
							<p>Navigate to:</p>
							<div class="code-block">
								<pre><code>edge://extensions</code></pre>
								<CopyButton text="edge://extensions" label="Copy Edge extensions URL" />
							</div>
						</div>
						<div class="platform-panel" id="browser-panel-brave" role="tabpanel" aria-labelledby="tab-browser-brave" data-tab="brave" hidden>
							<p>Navigate to:</p>
							<div class="code-block">
								<pre><code>brave://extensions</code></pre>
								<CopyButton text="brave://extensions" label="Copy Brave extensions URL" />
							</div>
						</div>
					</div>
				</li>

				<li class="step">
					<h3 class="step__title">
						<span class="step__number">4</span>
						Enable Developer Mode
					</h3>
					<p>
						Toggle <strong>Developer Mode</strong> on.
					</p>
					<p class="code-note" style="margin-inline-start: calc(2rem + var(--space-3));">
						<strong>Chrome &amp; Brave:</strong> The toggle is in the top-right corner of the page.<br />
						<strong>Edge:</strong> The toggle is in the left sidebar.
					</p>
				</li>

				<li class="step">
					<h3 class="step__title">
						<span class="step__number">5</span>
						Load the extension
					</h3>
					<p>
						Click the <strong>"Load unpacked"</strong> button that appears,
						then navigate to your extracted folder and select the <code>dist</code> directory.
					</p>
					<p><strong>That's it!</strong> Navigate to any Coursera quiz page and look for the 🎓 button in the bottom-right corner.</p>
				</li>
			</ol>
		</div>
	</section>

	<!-- ================================================================
	     NEXT STEPS
	     ================================================================ -->
	<section class="section" data-bg="deep" aria-labelledby="post-install-heading">
		<div class="container-narrow">
			<h2 id="post-install-heading">Next steps</h2>
			<ol class="next-steps-list">
				<li>Look for the <strong>🎓 button</strong> in your browser toolbar on any Coursera quiz page.</li>
				<li>Click the <strong>gear icon</strong> and paste an API key from any <a href="/docs/setup/">supported provider</a> — most offer free tiers.</li>
				<li>Open any Coursera quiz — the extension activates automatically.</li>
			</ol>
			<p class="next-steps-note">
				<strong>Load Unpacked extensions don't auto-update.</strong>
				Check the <a href={`https://github.com/${versionInfo.githubRepo}/releases`} target="_blank" rel="noopener noreferrer">releases page</a>
				periodically for new versions.
			</p>
		</div>
	</section>

	<!-- ================================================================
	     ALTERNATIVE INSTALLATION METHODS
	     ================================================================ -->
	<section class="section alt-methods" data-bg="void">
		<div class="container-narrow">
			<details id="alt-methods-heading" class="alt-methods-details">
				<summary>Alternative installation methods (terminal scripts &amp; app installers)</summary>

				<div class="alt-methods-content">
					<!-- Org notice — first thing visible when expanded -->
					<aside class="callout callout--accent" id="org-notice-heading" aria-labelledby="org-notice-title">
						<p class="callout__title" id="org-notice-title">
							You'll see "Managed by your organization" — that's normal
						</p>
						<p>
							Script and app methods install via browser policy. Chrome's extensions page will show
							<strong>"Managed by your organization"</strong>. This is normal —
							<strong>your browser has not been compromised</strong>. The policy only
							installs and enables this single extension. You can remove
							it at any time by running the
							<a href="#uninstall-heading">uninstall command</a>.
						</p>
					</aside>

					<h3 id="commands-heading">Terminal Install Scripts</h3>
					<p>Open a terminal and run the command for your platform.</p>

					<div class="install-tabs" data-tab-group="platform" role="tablist" aria-label="Install commands by platform">
						<button id="tab-install-linux" class="platform-tabs__tab" role="tab" data-tab="linux" aria-controls="install-panel-linux" aria-selected="false" tabindex="-1">Linux</button>
						<button id="tab-install-macos" class="platform-tabs__tab" role="tab" data-tab="macos" aria-controls="install-panel-macos" aria-selected="false" tabindex="-1">macOS</button>
						<button id="tab-install-windows" class="platform-tabs__tab" role="tab" data-tab="windows" aria-controls="install-panel-windows" aria-selected="false" tabindex="-1">Windows</button>
					</div>

					<!-- Linux -->
					<div class="platform-panel" id="install-panel-linux" role="tabpanel" aria-labelledby="tab-install-linux" data-tab="linux" hidden>
						<div class="code-block">
							<pre><code>curl -fsSL autocr.nicx.me/sh | sh</code></pre>
							<CopyButton text="curl -fsSL autocr.nicx.me/sh | sh" label="Copy Linux install command" />
						</div>
						<p class="code-note">
							<a href={`https://github.com/${versionInfo.githubRepo}/blob/master/website/public/scripts/install.sh`} target="_blank" rel="noopener noreferrer">View source</a> — always inspect scripts before running them.
						</p>
						<div class="callout callout--info callout--compact">
							<p class="callout__title">Requires sudo</p>
							<p>
								The install script needs root privileges to write browser policy files.
								When piping from <code>curl</code>, the script cannot prompt for your password — it will fail silently.
							</p>
							<p>
								Run with sudo directly:
							</p>
							<div class="code-block">
								<pre><code>curl -fsSL autocr.nicx.me/sh | sudo sh</code></pre>
								<CopyButton text="curl -fsSL autocr.nicx.me/sh | sudo sh" label="Copy Linux install command with sudo" />
							</div>
							<p>
								Or save and run the script manually:
							</p>
							<div class="code-block">
								<pre><code>curl -fsSL autocr.nicx.me/sh -o install.sh && chmod +x install.sh && sudo ./install.sh</code></pre>
								<CopyButton text="curl -fsSL autocr.nicx.me/sh -o install.sh && chmod +x install.sh && sudo ./install.sh" label="Copy Linux save-and-run command" />
							</div>
						</div>
						<details class="troubleshoot" id="faq-linux-troubleshoot">
							<summary>Troubleshooting</summary>
							<ul>
								<li><code>curl: command not found</code> — install curl with your package manager (<code>sudo apt install curl</code> on Debian/Ubuntu, <code>sudo dnf install curl</code> on Fedora).</li>
								<li><code>Permission denied</code> — the script will ask for <code>sudo</code> when it needs to write browser policy files. Make sure your user has sudo access.</li>
								<li>If Chrome/Brave/Edge isn't detected, the script installs the extension but can't apply the browser policy. See the <a href="/docs/manual/">manual install guide</a>.</li>
							</ul>
						</details>
					</div>

					<!-- macOS -->
					<div class="platform-panel" id="install-panel-macos" role="tabpanel" aria-labelledby="tab-install-macos" data-tab="macos" hidden>
						<div class="code-block">
							<pre><code>curl -fsSL autocr.nicx.me/mac | sh</code></pre>
							<CopyButton text="curl -fsSL autocr.nicx.me/mac | sh" label="Copy macOS install command" />
						</div>
						<p class="code-note">
							<a href={`https://github.com/${versionInfo.githubRepo}/blob/master/website/public/scripts/install-mac.sh`} target="_blank" rel="noopener noreferrer">View source</a> — always inspect scripts before running them.
						</p>
						<div class="callout callout--info callout--compact">
							<p class="callout__title">May require sudo</p>
							<p>
								The install script writes browser policy files, which may need root privileges.
								If you get a permission error, run with sudo:
							</p>
							<div class="code-block">
								<pre><code>curl -fsSL autocr.nicx.me/mac | sudo sh</code></pre>
								<CopyButton text="curl -fsSL autocr.nicx.me/mac | sudo sh" label="Copy macOS install command with sudo" />
							</div>
						</div>
						<details class="troubleshoot" id="faq-macos-troubleshoot">
							<summary>Troubleshooting</summary>
							<ul>
								<li>On Apple Silicon Macs, the script works identically — no Rosetta needed.</li>
								<li>If macOS Gatekeeper blocks the script, open <strong>System Settings &rarr; Privacy &amp; Security</strong> and allow the download.</li>
								<li>If the browser doesn't detect the extension after install, restart the browser completely (quit and reopen).</li>
							</ul>
						</details>
					</div>

					<!-- Windows -->
					<div class="platform-panel" id="install-panel-windows" role="tabpanel" aria-labelledby="tab-install-windows" data-tab="windows" hidden>
						<div class="code-block">
							<pre><code>irm autocr.nicx.me/ps | iex</code></pre>
							<CopyButton text="irm autocr.nicx.me/ps | iex" label="Copy Windows install command" />
						</div>
						<p class="code-note">
							<a href={`https://github.com/${versionInfo.githubRepo}/blob/master/website/public/scripts/install.ps1`} target="_blank" rel="noopener noreferrer">View source</a> — run in <strong>PowerShell</strong> (not Command Prompt).
						</p>
						<details class="troubleshoot" id="faq-windows-troubleshoot">
							<summary>Troubleshooting</summary>
							<ul>
								<li><code>irm: The term 'irm' is not recognized</code> — you need PowerShell 5.1+. Open PowerShell from the Start Menu, not cmd.exe.</li>
								<li>If UAC prompts for admin access, click <strong>Yes</strong> — the script needs it to write browser policy registry keys.</li>
								<li>Windows Defender SmartScreen may warn about the script. Click <strong>More info &rarr; Run anyway</strong>.</li>
							</ul>
						</details>
					</div>

					<h3>App Installers</h3>
					<p>
						Platform-specific installer apps are available on the
						<a href="/downloads/">downloads page</a>.
					</p>
				</div>
			</details>
		</div>
	</section>

	<!-- ================================================================
	     PERMISSIONS
	     ================================================================ -->
	<section class="section" data-bg="deep" aria-labelledby="permissions-heading">
		<div class="container-narrow">
			<h2 id="permissions-heading">What permissions does it need?</h2>
			<p>
				Auto-Coursera requests only the permissions it needs to
				function. Here's a quick summary:
			</p>

			<table class="permissions-table">
				<thead>
					<tr>
						<th>Permission</th>
						<th>Why</th>
					</tr>
				</thead>
				<tbody>
					<tr>
						<td><code>activeTab</code></td>
						<td>Read quiz questions from the Coursera page you're viewing</td>
					</tr>
					<tr>
						<td><code>alarms</code></td>
						<td>Schedule periodic update checks (every 6 hours)</td>
					</tr>
					<tr>
						<td><code>storage</code></td>
						<td>Save your settings and encrypted API keys locally</td>
					</tr>
					<tr>
						<td><code>tabs</code></td>
						<td>Open the settings page on first install</td>
					</tr>
					<tr>
						<td>Coursera hosts</td>
						<td>Run the content script on Coursera quiz pages</td>
					</tr>
					<tr>
						<td>Update server</td>
						<td>Check for and download extension updates</td>
					</tr>
					<tr>
						<td>AI provider APIs</td>
						<td>Send quiz questions to the AI provider you configured</td>
					</tr>
					<tr>
						<td>Image CDNs</td>
						<td>Download question images hosted on Coursera's CDNs</td>
					</tr>
				</tbody>
			</table>

			<p class="permissions-link">
				For detailed privacy explanations of each permission, see our
				<a href="/privacy/#permissions-heading">Privacy Policy</a>.
			</p>
		</div>
	</section>

	<!-- ================================================================
	     UNINSTALL
	     ================================================================ -->
	<section class="section" data-bg="void" aria-labelledby="uninstall-heading">
		<div class="container-narrow">
			<h2 id="uninstall-heading">Uninstall</h2>

			<h3>Installed via Load Unpacked</h3>
			<p>
				Open your browser's extensions page
				(<code>chrome://extensions</code> in Chrome), find
				Auto-Coursera, and click <strong>Remove</strong>.
				Then delete the extracted folder from your computer.
			</p>

			<details class="uninstall-details" id="uninstall-scripts">
				<summary>Installed via terminal scripts? Uninstall commands here</summary>
				<div class="uninstall-content">
					<h4>Linux</h4>
					<div class="code-block">
						<pre><code>curl -fsSL autocr.nicx.me/sh | sh -s -- --uninstall</code></pre>
						<CopyButton text="curl -fsSL autocr.nicx.me/sh | sh -s -- --uninstall" label="Copy uninstall command for Linux" />
					</div>

					<h4>macOS</h4>
					<div class="code-block">
						<pre><code>curl -fsSL autocr.nicx.me/mac | sh -s -- --uninstall</code></pre>
						<CopyButton text="curl -fsSL autocr.nicx.me/mac | sh -s -- --uninstall" label="Copy uninstall command for macOS" />
					</div>

					<h4>Windows</h4>
					<div class="code-block">
						<pre><code>irm autocr.nicx.me/ps-uninstall | iex</code></pre>
						<CopyButton text="irm autocr.nicx.me/ps-uninstall | iex" label="Copy uninstall command for Windows" />
					</div>
				</div>
			</details>
		</div>
	</section>

	<!-- ================================================================
	     FAQ
	     ================================================================ -->
	<section class="section" data-bg="deep" aria-labelledby="faq-heading">
		<div class="container-narrow">
			<h2 id="faq-heading">Frequently asked questions</h2>

			<details class="faq-item" id="faq-not-on-chrome-store">
				<summary>Why isn't this on the Chrome Web Store?</summary>
				<div class="faq-body">
					<p>
						Chrome Web Store policies prohibit extensions that
						interact with academic content in this way. Auto-Coursera
						is distributed as a sideloaded extension using browser
						policy — the same mechanism enterprises use to deploy
						internal tools. The installer script handles this
						automatically.
					</p>
					<p>
						The full source code is available on
						<a href={`https://github.com/${versionInfo.githubRepo}`} target="_blank" rel="noopener noreferrer">GitHub</a>.
						You can audit every line before installing.
					</p>
				</div>
			</details>

			<details class="faq-item" id="faq-is-it-free">
				<summary>Is it really free?</summary>
				<div class="faq-body">
					<p>
						The extension itself is completely free and open source.
						You provide your own API key for the AI provider, and
						most providers offer free tiers that are more than enough
						for quiz use.
					</p>
				</div>
			</details>

			<details class="faq-item" id="faq-auto-update">
				<summary>Does it auto-update?</summary>
				<div class="faq-body">
					<p>
						<strong>If you installed via Load Unpacked</strong>, the
						extension does not auto-update. Check our
						<a href={`https://github.com/${versionInfo.githubRepo}/releases`} target="_blank" rel="noopener noreferrer">releases page</a>
						for new versions, download the latest zip, replace the
						files, and click reload on the extensions page.
					</p>
					<p>
						<strong>If you installed via terminal scripts or app
						installer</strong>, the extension checks for updates every
						6 hours automatically.
					</p>
				</div>
			</details>

			<details class="faq-item" id="faq-firefox-support">
				<summary>Can I use it on Firefox?</summary>
				<div class="faq-body">
					<p>
						Not currently. Auto-Coursera uses Chrome extension APIs
						(Manifest V3) and Chromium-specific features. Firefox
						support would require a separate build with different
						APIs.
					</p>
				</div>
			</details>

			<details class="faq-item" id="faq-why-load-unpacked">
				<summary>Why Load Unpacked instead of the install script?</summary>
				<div class="faq-body">
					<p>
						Load Unpacked works reliably across all platforms without
						requiring terminal access, admin privileges, or browser
						policy modifications. The terminal scripts use browser
						policy installation which can trigger security warnings
						and may not work on all systems.
					</p>
				</div>
			</details>
		</div>
	</section>
</Base>

<script>
	import { initTabs, navigateToHash, setupHashListeners } from '../../scripts/tabs';

	const TAB_GROUPS = [
		{
			group: 'browser',
			defaultTab: 'chrome',
		},
		{
			group: 'platform',
			defaultTab: (() => {
				const ua = navigator.userAgent.toLowerCase();
				if (ua.includes('win')) return 'windows';
				if (ua.includes('mac')) return 'macos';
				return 'linux';
			})(),
		},
	];

	function init() {
		initTabs(TAB_GROUPS);
		navigateToHash();
	}

	if (document.readyState === 'loading') {
		document.addEventListener('DOMContentLoaded', init);
	} else {
		init();
	}

	setupHashListeners();
</script>

<style>
	/* ==========================================================================
	   Install Page — Scoped Styles
	   ========================================================================== */

	/* ===== Hero ===== */

	.hero {
		padding-block-start: calc(var(--nav-height) + var(--space-7));
		padding-block-end: var(--space-6);
		background-color: var(--bg-deep);
	}

	.hero h1 {
		margin-block-end: var(--space-4);
	}

	.hero__product {
		font-family: var(--font-mono);
		font-size: var(--text-small);
		color: var(--text-muted);
		text-transform: uppercase;
		letter-spacing: 0.05em;
		margin-block-end: var(--space-2);
	}

	.hero__meta {
		display: flex;
		align-items: center;
		gap: var(--space-3);
		margin-block-end: var(--space-5);
		flex-wrap: wrap;
	}

	.version-badge {
		font-family: var(--font-mono);
		font-size: var(--text-small);
		font-weight: var(--weight-semibold);
		color: var(--accent);
		background-color: var(--accent-subtle);
		padding: var(--space-1) var(--space-3);
		border-radius: var(--border-radius-sm);
		line-height: 1;
	}

	.hero__separator {
		color: var(--text-muted);
	}

	.hero__note {
		font-size: var(--text-small);
		color: var(--text-muted);
	}

	.hero__lead {
		font-size: clamp(1.0625rem, 1rem + 0.25vw, 1.1875rem);
		max-width: 36rem;
	}

	.hero__lead strong {
		color: var(--text-primary);
		font-weight: var(--weight-semibold);
	}

	.hero__actions {
		display: flex;
		align-items: center;
		gap: var(--space-4);
		margin-block-start: var(--space-5);
		flex-wrap: wrap;
	}

	/* ===== Sections ===== */

	.section {
		padding-block: var(--space-6);
	}

	.section--flush {
		padding-block: 0 var(--space-5);
		display: flow-root; /* BFC prevents child margins collapsing through */
	}

	.section h2 {
		margin-block-end: var(--space-4);
	}

	.section h3 {
		margin-block-start: var(--space-6);
		margin-block-end: var(--space-3);
	}

	.section h3:first-child,
	.section h2 + p + h3 {
		margin-block-start: var(--space-5);
	}

	/* ===== Platform / Install Tabs ===== */

	.platform-tabs,
	.install-tabs {
		display: flex;
		gap: 0;
		border-bottom: 1px solid var(--border);
		margin-block-start: var(--space-5);
	}

	.platform-tabs__tab {
		font-family: var(--font-mono);
		font-size: var(--text-small);
		font-weight: var(--weight-medium);
		color: var(--text-muted);
		background: none;
		border: none;
		padding: var(--space-2) var(--space-4);
		cursor: pointer;
		border-bottom: 2px solid transparent;
		margin-bottom: -1px;
		transition:
			color var(--transition-fast),
			border-color var(--transition-fast);
	}

	.platform-tabs__tab:hover {
		color: var(--text-secondary);
	}

	.platform-tabs__tab[aria-selected='true'] {
		color: var(--accent);
		border-bottom-color: var(--accent);
	}

	.platform-panel {
		background-color: var(--surface);
		border: 1px solid var(--border);
		border-top: none;
		border-radius: 0 0 var(--border-radius) var(--border-radius);
		padding: var(--space-4) var(--space-5);
	}

	.platform-panel[hidden] {
		display: none;
	}

	/* ===== Code Blocks ===== */

	.code-block {
		display: flex;
		align-items: center;
		gap: var(--space-3);
		flex-wrap: wrap;
	}

	.code-block pre {
		flex: 1;
		min-width: 0;
		margin: 0;
		border: 1px solid var(--border);
		border-inline-start: 3px solid var(--accent);
	}

	.platform-panel .code-block pre {
		background-color: var(--bg-deep);
		border: 1px solid var(--border);
		border-inline-start: 3px solid var(--accent);
	}

	.code-note {
		font-size: var(--text-small);
		color: var(--text-muted);
		margin-block-start: var(--space-2);
	}

	/* ===== Troubleshooting Dropdowns ===== */

	.troubleshoot {
		margin-block-start: var(--space-4);
		border: 1px solid var(--border);
		border-radius: var(--border-radius);
		background-color: var(--bg-deep);
	}

	.troubleshoot summary {
		padding: var(--space-3) var(--space-4);
		font-weight: var(--weight-semibold);
		font-size: var(--text-small);
		color: var(--text-secondary);
		cursor: pointer;
		list-style: none;
	}

	.troubleshoot summary::before {
		content: '▸ ';
		color: var(--text-muted);
	}

	.troubleshoot[open] summary::before {
		content: '▾ ';
	}

	.troubleshoot summary::-webkit-details-marker {
		display: none;
	}

	.troubleshoot ul {
		padding: 0 var(--space-4) var(--space-4) var(--space-4);
		margin: 0;
		padding-inline-start: calc(var(--space-4) + var(--space-5));
	}

	.troubleshoot li {
		font-size: var(--text-small);
		color: var(--text-secondary);
		margin-block: var(--space-2);
	}

	/* ===== Steps ===== */

	.steps {
		list-style: none;
		padding: 0;
		margin-block-start: var(--space-5);
		display: flex;
		flex-direction: column;
		gap: var(--space-6);
	}

	.step {
		margin: 0;
	}

	.step__title {
		display: flex;
		align-items: center;
		gap: var(--space-3);
		margin-block-end: var(--space-2);
	}

	.step__number {
		display: inline-flex;
		align-items: center;
		justify-content: center;
		width: 2rem;
		height: 2rem;
		font-family: var(--font-mono);
		font-size: var(--text-small);
		font-weight: var(--weight-bold);
		color: var(--accent);
		background-color: var(--accent-subtle);
		border-radius: 50%;
		flex-shrink: 0;
		line-height: 1;
	}

	.step p {
		margin-inline-start: calc(2rem + var(--space-3));
	}

	/* ===== Permissions Table ===== */

	.permissions-table {
		width: 100%;
		margin-block-start: var(--space-5);
		border-collapse: collapse;
		font-size: var(--text-body);
	}

	.permissions-table th,
	.permissions-table td {
		padding: var(--space-3) var(--space-4);
		text-align: left;
		border-bottom: 1px solid var(--border);
	}

	.permissions-table thead th {
		font-weight: var(--weight-semibold);
		color: var(--text-primary);
		font-size: var(--text-small);
		text-transform: uppercase;
		letter-spacing: 0.04em;
		background-color: var(--surface);
	}

	.permissions-table tbody td {
		color: var(--text-secondary);
		line-height: var(--leading-normal);
	}

	.permissions-table tbody td:first-child {
		font-weight: var(--weight-medium);
		color: var(--text-primary);
		white-space: nowrap;
	}

	.permissions-table tbody tr:last-child td {
		border-bottom: none;
	}

	.permissions-link {
		margin-block-start: var(--space-4);
		font-size: var(--text-small);
		color: var(--text-muted);
	}

	/* ===== FAQ ===== */

	details[id] {
		scroll-margin-top: calc(var(--nav-height) + var(--space-4));
	}

	.faq-item {
		border: 1px solid var(--border);
		border-radius: var(--border-radius);
		margin-block-end: var(--space-3);
		background-color: var(--surface);
	}

	.faq-item summary {
		padding: var(--space-4) var(--space-5);
		font-weight: var(--weight-semibold);
		color: var(--text-primary);
		cursor: pointer;
		list-style: none;
	}

	.faq-item summary::before {
		content: '▸ ';
		color: var(--text-muted);
	}

	.faq-item[open] summary::before {
		content: '▾ ';
	}

	.faq-item summary::-webkit-details-marker {
		display: none;
	}

	.faq-body {
		padding: 0 var(--space-5) var(--space-5);
	}

	.faq-body p {
		margin-block-end: var(--space-3);
	}

	.faq-body p:last-child {
		margin-block-end: 0;
	}

	/* ===== Callout Boxes ===== */

	.callout {
		padding: var(--space-4) var(--space-5);
		border-radius: var(--border-radius);
		border: 1px solid var(--border);
		background-color: var(--surface);
	}

	.callout__title {
		font-weight: var(--weight-semibold);
		color: var(--text-primary);
		margin-block-end: var(--space-2);
	}

	.callout p {
		color: var(--text-secondary);
		font-size: var(--text-body);
		line-height: var(--leading-normal);
	}

	.callout p + p {
		margin-block-start: var(--space-3);
	}

	/* Warning variant — amber left border */
	.callout--warning {
		border-inline-start: 3px solid var(--warning);
	}

	.callout--warning .callout__title {
		color: var(--warning);
	}

	/* Accent variant — amber left border, for important notices */
	.callout--accent {
		border-inline-start: 3px solid var(--accent);
	}

	.callout--accent .callout__title {
		color: var(--accent);
	}

	/* Info variant — neutral, for contextual tips */
	.callout--info {
		border-inline-start: 3px solid var(--text-muted);
		background-color: var(--bg-deep);
	}

	.callout--info .callout__title {
		color: var(--text-secondary);
		font-size: var(--text-small);
	}

	.callout--info p {
		font-size: var(--text-small);
	}

	/* Compact variant — used inside panels */
	.callout--compact {
		margin-block-start: var(--space-4);
		padding: var(--space-3) var(--space-4);
	}

	.callout--compact .code-block {
		margin-block-start: var(--space-2);
	}

	.callout--compact .code-block pre {
		border: 1px solid var(--border);
		border-inline-start: 3px solid var(--accent);
		background-color: var(--surface);
	}

	/* Restart variant — red/error left border, high urgency */
	.callout--restart {
		border-inline-start: 3px solid var(--error);
		background-color: var(--error-subtle);
	}

	.callout--restart .callout__title {
		color: var(--error);
	}

	/* ===== Alternative Methods ===== */

	.alt-methods {
		border-top: 1px solid var(--border);
		padding-top: var(--space-8);
	}

	/* ===== Alt Methods Details ===== */

	.alt-methods-details {
		border: 1px solid var(--border);
		border-radius: var(--border-radius);
		background-color: var(--surface);
	}

	.alt-methods-details summary {
		padding: var(--space-4) var(--space-5);
		font-weight: var(--weight-semibold);
		font-size: 1.125rem;
		color: var(--text-secondary);
		cursor: pointer;
		list-style: none;
	}

	.alt-methods-details summary::before {
		content: '▸ ';
		color: var(--text-muted);
	}

	.alt-methods-details[open] summary::before {
		content: '▾ ';
	}

	.alt-methods-details summary::-webkit-details-marker {
		display: none;
	}

	.alt-methods-content {
		padding: var(--space-4) var(--space-5);
		border-top: 1px solid var(--border);
	}

	.alt-methods-content .callout {
		margin-block-end: var(--space-5);
	}

	/* ===== Uninstall Details ===== */

	.uninstall-details {
		margin-block-start: var(--space-5);
		border: 1px solid var(--border);
		border-radius: var(--border-radius);
		background-color: var(--surface);
	}

	.uninstall-details summary {
		padding: var(--space-3) var(--space-4);
		font-weight: var(--weight-semibold);
		font-size: var(--text-small);
		color: var(--text-secondary);
		cursor: pointer;
		list-style: none;
	}

	.uninstall-details summary::before {
		content: '▸ ';
		color: var(--text-muted);
	}

	.uninstall-details[open] summary::before {
		content: '▾ ';
	}

	.uninstall-details summary::-webkit-details-marker {
		display: none;
	}

	.uninstall-content {
		padding: 0 var(--space-4) var(--space-4);
	}

	/* ===== Next Steps ===== */

	.next-steps-list {
		padding-inline-start: var(--space-5);
		margin-block: var(--space-4);
		display: flex;
		flex-direction: column;
		gap: var(--space-3);
	}

	.next-steps-list li {
		line-height: var(--leading-normal);
		color: var(--text-secondary);
	}

	.next-steps-note {
		font-size: var(--text-small);
		color: var(--text-muted);
		margin-block-start: var(--space-4);
	}

	/* ===== Install Flow Illustration ===== */

	.install-flow-img {
		display: block;
		width: 100%;
		max-width: 640px;
		height: auto;
		margin-block: var(--space-4) var(--space-2);
		opacity: 0.85;
	}

	/* ===== Responsive ===== */

	@media (max-width: 480px) {
		.hero {
			padding-block-start: calc(var(--nav-height) + var(--space-6));
			padding-block-end: var(--space-5);
		}

		.hero__actions {
			flex-direction: column;
			align-items: flex-start;
		}

		.code-block {
			flex-direction: column;
			align-items: stretch;
		}

		pre {
			font-size: clamp(0.7rem, 2.5vw, var(--text-code));
			word-break: break-all;
			white-space: pre-wrap;
		}

		.step p {
			margin-inline-start: 0;
			margin-block-start: var(--space-2);
		}

		.step__tabbed {
			margin-inline-start: 0 !important;
		}

		.step .callout--compact {
			margin-inline-start: 0 !important;
		}

		.step .code-note {
			margin-inline-start: 0 !important;
		}
	}
</style>
